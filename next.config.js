/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Eliminar la configuración de i18n
  // i18n: {
  //   locales: ['en', 'es'],  // Define las lenguas aquí también
  //   defaultLocale: 'en',
  //   localeDetection: false,  // Si quieres desactivar la detección automática de lenguas
  // },
};

module.exports = nextConfig;